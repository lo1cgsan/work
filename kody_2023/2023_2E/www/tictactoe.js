function setup() {
    createCanvas(720, 400);

    // kolor tła
    background(223, 199, 199);

    // kolor wypełnienia
    fill(255, 0, 0);

    // kolor konkturu
    stroke(0);
}

function draw() {
    rect(40, 120, 120, 40);
}
