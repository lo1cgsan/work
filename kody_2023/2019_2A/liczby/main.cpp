#include <iostream>

using namespace std;

int main()
{
    float a, b, c;
    cin >> a >> b >> c;
    if (a+b>c)
        if (a+c>b)
            if(b+c>a) {
                cout << "Trójkąt";
            else
    }
    && – logiczny operator koniunkcji
    || – logiczny operator alternatywy
    return 0;
}
